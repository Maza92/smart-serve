export interface CashRegister {
  id: number;
  user: string;
  openDate: string;
  closeDate: string;
  initialAmount: string;
  finalAmount: string;
  notes: string;
  status: string;
  createdAt: string;
}
